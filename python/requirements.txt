# The Big Drone Detector runs on the Python standard library alone — no pip
# install is strictly required.
#
# The one optional dependency turns it into a standalone DESKTOP APP window
# instead of a browser tab. Once it's installed, `python drone_detector.py`
# opens the app in its own window automatically (DDX_NATIVE=auto, the default).
# Force the window with DDX_NATIVE=1, or force the browser with DDX_NATIVE=0.
pywebview>=5.0
